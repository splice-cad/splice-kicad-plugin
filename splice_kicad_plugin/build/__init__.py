"""PlanData assembly — stable IDs and ExtractedConnector → PlanData JSON.

Submodules:
- ``ids``  — deterministic UUID5-derived node / pin / BOM IDs (round-trip safe).
- ``plan`` — assembles the PlanData document POSTed to ``/api/plans/import``.
"""
