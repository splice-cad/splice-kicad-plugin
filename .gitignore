# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
/dist/
# Anchored: ignore the repo-root Python build-output dir and the PCM staging
# dir, but NOT the source subpackage `splice_kicad_plugin/build/` (a generic
# `build/` rule silently un-tracked it once — that broke a release).
/build/
/packaging/build/
.venv/
venv/

# Type checkers / linters
.mypy_cache/
.pyright_cache/
.ruff_cache/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# Vendored deps (built at packaging time)
splice_kicad_plugin/_vendor/

# Test outputs
tests/output/
*.actual.json
